# ─── core runtime ─────────────────────────────────────
httpx==0.23.3
solana==0.29.0
base58==2.1.1
pydantic==1.10.14
requests==2.32.2

# ─── numerical / ML  (used by scoring_engine & factor_loader) ───
numpy==1.26.4
torch==2.2.2+cpu      # CPU‑only build (~260 MB)

# ─── test helpers ─────────────────────────────────────
pytest==8.4.0
pytest-asyncio==1.0.0


# ── phase‑11 extra libs ───────────────────────────
aiohttp==3.9.5           # async sentiment + whale stream
pyyaml==6.0.1            # param files
heliuspy==0.3.2          # wallet flow stream (Solana “Helius” API)
