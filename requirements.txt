# ── core runtime ─────────────────────────────────────────
httpx==0.23.3
requests==2.32.2
pydantic==1.10.14
base58==2.1.1
aiohttp==3.9.5          # for discord_notifier
pandas==2.2.2           # factor_loader  (Py311 wheels)
pyarrow==15.0.2         # pandas ≥2 needs this wheel

# ── Solana stack ─────────────────────────────────────────
solana==0.29.0          # pulls solders (Py311 wheel)

# ── numeric / ML ─────────────────────────────────────────
numpy==1.26.4
torch==2.2.2+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# … existing lines …

PyYAML==6.0.1          # YAML loader for atomic_arb params

PyYAML==6.0.2
PyYAML==6.0.2
backoff==2.2.1
solana==0.29.0
